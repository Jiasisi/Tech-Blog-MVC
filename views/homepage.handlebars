<section>
    {{#each blogs as |blog|}}
    <div class="basic-card">
        <div class="basic-card-head">
            <h3>{{blog.blog_content}}</h3>
            <p>POsted by {{userName}} on {{formate_date publish_date}}</p>
        
        </div>
        <div class="basic-body">
            <p>{{blog.blog_content}}</p>
        </div>
    </div>

    
    {{/each}} 
</section>